# Runtime resources

The Python files and prompt resources in this directory were recovered from the
selected submission image.

The merged model is intentionally excluded from Git. Run:

```bash
python scripts/download_weights.py
```

from the repository root to populate `resources/base/` from the public
Hugging Face release.
